/**
 * HeroSection — placeholder for Phase 2.
 * Will be wired up to the existing Hero component or rebuilt.
 */
export function HeroSection() {
  return (
    <section id="hero-section" className="relative">
      {/* Phase 2: Hero content */}
    </section>
  );
}
